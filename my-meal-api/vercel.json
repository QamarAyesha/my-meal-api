{
  "builds": [{
    "src": "api/index.py",
    "use": "@vercel/python",
    "config": {
      "maxLambdaSize": "50mb",
      "suppressLoaders": true,
      "externalNodeModules": ["numpy"]  # Prevents duplicate installation
    }
  }]
}
